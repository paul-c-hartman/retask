# Retask

## Development

To run this app, you'll need to install [tauri and related dependencies](https://tauri.app/start/prerequisites/). Then:

1. Clone the repository
2. `cd retask`
3. `npm install`
4. To develop for desktop: `npm run tauri dev`
5. To develop for Android: `npm run tauri android dev`
